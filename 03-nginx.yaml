- name: nginx install and run
  hosts: frontend
  become: yes # sudo access
  
  tasks:
  - name: install nginx server
    ansible.builtin.package: # install package
      name: nginx
      state: present
  
  - name: start nginx
    ansible.builtin.service: # manage service
      name: nginx
      state: started
      enabled: yes